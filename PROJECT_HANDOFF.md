# Project Handoff: loyalty-app

ใช้เอกสารนี้เป็นบริบทเริ่มต้นเมื่อต้องกลับมาพัฒนาโปรเจกต์ต่อ

## ภาพรวม

โปรเจกต์เป็นเว็บ Flask สำหรับระบบสมาชิกและหวยออนไลน์ของแบรนด์ "สมบัติสี่จักรพรรดิ" มีระบบห้องหวย, รอบหวย, โพย, ผลรางวัล, เครดิต, ฝากถอน, Partner และหลังบ้าน Admin

ขอบเขตปัจจุบันเน้นทำระบบหวยให้สมบูรณ์ ส่วนเกมสล็อตและแทงบอลถูกพักไว้ก่อน

## สิทธิ์ผู้ใช้ ห้ามเปลี่ยนโดยไม่ได้รับอนุญาต

- `admin`: เข้าหลังบ้านและตรวจสอบ/จัดการข้อมูลทั้งหมด
- user ปกติ: ใช้งานเฉพาะหน้าสมาชิกและข้อมูลของตัวเอง
- `partner`: เข้าเฉพาะหน้า Partner ของตัวเอง ไม่เข้าหลังบ้าน Admin
- บัญชีเริ่มต้นที่ต้องคงไว้: `admin / admin1234`
- บัญชี `admin2` ถูกเอาออกตามคำสั่งเจ้าของโปรเจกต์ (2026-09-26) และระบบจะไม่สร้างกลับมา

ห้ามเปลี่ยนหรือลบบัญชีและโครงสร้างสิทธิ์ข้างต้น เว้นแต่เจ้าของโปรเจกต์สั่งโดยตรง

## สิ่งที่ทำเสร็จแล้ว

- ระบบห้องหวยและรอบหวย พร้อมเวลาเปิด/ปิดรับ
- ตรวจเลขหวยและจำนวนหลักฝั่ง Server
- ตรวจยอดโพยรวมก่อนหักเครดิต
- ป้องกันเลขซ้ำ, จำกัดจำนวนรายการและยอดต่อรายการ
- รางวัลหวยจ่ายเข้ากระเป๋าเครดิต
- settlement ผลหวย, audit log และ reverse ผลอย่างปลอดภัย
- ใบโพยสมาชิก และหน้า Admin ดูโพยสมาชิกทั้งหมด
- กรองโพย Admin ตามงวด, สมาชิก และสถานะ
- responsive header และ UI ธีมทอง/น้ำเงิน/ไวน์ พร้อม glow
- เกม Demo เปิดหีบสมบัติ ไม่เกี่ยวกับเครดิตจริงหรือการถอนเงิน
- หน้าเลือกห้องหวยจัดหมวดหมู่และรองรับรูป/ธงจาก `image_url`
- ปุ่มฝากเงิน/เริ่มแทงหวย/ถอนเงินบนหน้าแรก
- แยกห้อง `/deposit` และ `/withdraw`
- ฝากเงินใช้บัญชีรับฝากที่ Admin ตั้งค่าได้เอง
- รองรับอัปโหลดสลิป JPG, JPEG, PNG, WEBP
- Admin ตั้งค่าสีบัญชีรับฝากได้
- Logged out ซ่อนข้อมูลและปุ่มสำหรับสมาชิก

## เส้นทางสำคัญ

- หน้าแรก: `/`
- ห้องหวย: `/lottery/rooms`
- แทงหวย: `/lottery/thai`
- ผลหวย: `/results`
- ฝากเงิน: `/deposit`
- ถอนเงิน: `/withdraw`
- กระเป๋าเดิม: `/wallet`
- เกม Demo: `/games/treasure-chest`
- หลังบ้าน: `/admin`
- จัดการผล/อัตราจ่ายหวย: `/admin/thai-lottery`
- ดูโพยสมาชิก: `/admin/lottery-tickets`
- ตั้งค่าบัญชีรับฝาก: `/admin/deposit-account`

## คำสั่งรันในเครื่อง

เปิด PowerShell ที่ `c:\Users\User\loyalty-app`

```powershell
python app.py
```

เว็บในเครื่อง: `http://127.0.0.1:5000`

ทดสอบ:

```powershell
python -m pytest -q
python -m py_compile app.py models.py
```

ผลล่าสุดที่ยืนยัน: `10 passed` พร้อม warning เดิมจาก `datetime.utcnow()` ของ SQLAlchemy

## ลิงก์ภายนอกชั่วคราว

ใช้ Cloudflare Quick Tunnel เมื่อจำเป็นต้องให้เครื่องอื่นเปิดดู:

```powershell
.\cloudflared-windows-amd64.exe tunnel --url http://127.0.0.1:5000
```

ลิงก์เดิมที่เคยสร้าง: `https://artists-plugin-automation-glass.trycloudflare.com`

Quick Tunnel เป็นลิงก์ชั่วคราว อาจเปลี่ยนเมื่อรันใหม่ และใช้ได้เฉพาะขณะที่ Flask กับ cloudflared ยังทำงานอยู่

## Prompt สำหรับกลับมาทำงานต่อ

คัดลอก prompt นี้ใช้เริ่ม session ใหม่:

```text
เรากำลังพัฒนาต่อใน workspace c:\Users\User\loyalty-app โปรเจกต์นี้เป็น Flask เว็บหวยของแบรนด์ "สมบัติสี่จักรพรรดิ"

กรุณาอ่าน PROJECT_HANDOFF.md และ /memories/repo/notes.md ก่อนเริ่มงาน แล้วตรวจสถานะโค้ดเฉพาะส่วนที่เกี่ยวข้องกับคำขอใหม่

ข้อกำหนดสำคัญ:
- โฟกัสเฉพาะระบบหวยก่อน สล็อตและแทงบอลพักไว้
- Admin มีสิทธิ์หลังบ้านทั้งหมด
- user ปกติไม่มีสิทธิ์หลังบ้าน
- Partner เข้าได้เฉพาะหน้า Partner ของตัวเอง
- ห้ามเปลี่ยนหรือลบบัญชี admin โดยไม่ได้รับอนุญาต (admin2 ถูกเอาออกแล้วตามคำสั่งเจ้าของโปรเจกต์)
- รักษาการแยกเครดิต, แต้ม, ฝากเงิน, ถอนเงิน และสิทธิ์ผู้ใช้
- ก่อนแก้ให้หาจุดควบคุมใกล้ที่สุดและตั้งสมมติฐานที่ตรวจสอบได้
- แก้ให้น้อยไฟล์ที่สุด ใช้ apply_patch
- หลังแก้ให้รัน test ที่แคบที่สุดก่อน แล้วรัน python -m pytest -q และ py_compile เมื่อเหมาะสม
- ห้าม commit หรือสร้าง branch เอง

งานล่าสุดที่ทำเสร็จแล้วอยู่ใน PROJECT_HANDOFF.md ให้ตรวจโค้ดปัจจุบันก่อนเสนอการเปลี่ยนแปลง และสรุปผลพร้อมไฟล์ที่แก้และผลทดสอบตอนจบ
```

## ข้อควรทำเมื่อกลับมา

1. ตรวจ `git status` ก่อนแก้ เพราะ workspace มีการเปลี่ยนแปลงเดิมจำนวนมาก
2. อย่าย้อนหรือลบไฟล์ที่ผู้ใช้แก้ไว้
3. ระบบยังมีประเด็นที่ควรทำภายหลัง เช่น CSRF, validation upload ให้เข้มขึ้น, Decimal แทน Float และ production secret
4. อย่าเปิดเกม Demo ให้เกี่ยวกับเงินจริงโดยไม่ตรวจข้อกฎหมายและระบบใบอนุญาตก่อน
